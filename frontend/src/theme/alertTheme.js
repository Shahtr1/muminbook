const alertTheme = {
  baseStyle: {
    container: {
      borderRadius: "sm",
    },
  },
  variants: {
    subtle: (props) => ({
      container: {
        borderRadius: "sm",
      },
    }),
    solid: (props) => ({
      container: {
        borderRadius: "sm",
      },
    }),
    leftAccent: (props) => ({
      container: {
        borderRadius: "sm",
      },
    }),
    topAccent: (props) => ({
      container: {
        borderRadius: "sm",
      },
    }),
  },
};

export default alertTheme;
