// import { test } from '@playwright/test';
// import { explorer } from '../actions/explorer-actions';
//
// test.describe('Pin Explorer Lifecycle', () => {
//   test('pin folder → appears in sidebar', async ({ page }) => {});
//
//   test('unpin folder → removed from sidebar', async ({ page }) => {});
//
//   test('pinned folder → rename → sidebar reflects new name', async ({
//     page,
//   }) => {});
//
//   test('pinned folder → trash → removed from sidebar', async ({ page }) => {});
//
//   test('pinned folder → trash → restore → reappears pinned', async ({
//     page,
//   }) => {});
//
//   test('pinned folder → permanent delete → removed permanently', async ({
//     page,
//   }) => {});
//
//   test('restore all → restores pinned state correctly', async ({ page }) => {});
//
//   test('empty trash → pinned items removed permanently', async ({
//     page,
//   }) => {});
// });
